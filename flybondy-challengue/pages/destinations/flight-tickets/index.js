import React from 'react';
import TicketList from '../../../components/flight-tickets';
import withContainer from '../../../hocs/withContainer';

const Index = () => <TicketList />;

export default withContainer(Index);
